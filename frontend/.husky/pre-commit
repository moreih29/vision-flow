#!/bin/sh

# Frontend
cd frontend && npx lint-staged

# Backend (skip if ruff not installed)
cd ../backend
if [ -x .venv/bin/ruff ]; then
  .venv/bin/ruff check app/ tests/ && .venv/bin/ruff format --check app/ tests/
fi
